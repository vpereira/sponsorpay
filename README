TODO:
 drop all views, build a backbone.js layer to talk json with our API proxy

Before to run it:
bundle install
To run the tests:

    rspec spec

To try the application

ruby sponsorpay.rb -sv

It will be listening in the port 9000